<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.practice.domain.seller.SellerDao">

<insert id="insert" useGeneratedKeys="true" keyProperty="sellerId">
    insert into seller(seller_name, seller_email, users_id, created_at)
    values(#{sellerName}, #{sellerEmail}, #{usersId}, NOW())
</insert>

</mapper>